# Feedback artifacts

- `FEEDBACK_TEMPLATE.md` — copy per session as `FEEDBACK_YYYY-MM-DD.md`
- `CONFIRM_*.md` — written by `Confirm-GSignalX.ps1 -WriteFeedback`

Do not commit secrets (account numbers, server passwords). Heartbeat JSON may contain login/server — scrub if sharing externally.
